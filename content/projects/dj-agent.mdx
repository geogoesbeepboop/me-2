---
title: "DJ Agent"
summary: "A personal DJ that turns my real library into a vibe database, learns my taste from how I annotate tracks, and plans beatmatched sets a deterministic critic must pass before any audio renders."
question: "Can we leverage agentic intelligence to curate an authentic vibe?"
date: "2026-06-01"
started: "2026-06-01"
updated: "2026-07-26"
status: "LIVE"
domain: "AUDIO · ML"
accent: "#c77dff"
stage: "bench"
repo: "/Users/geoandr/dev/dj-agent"
refs:
  - "writing/loop-engineering#the-inner-loop"
  - "writing/agent-anatomy#risk-is-blast-radius"
  - "writing/evals-are-the-product"
reflection: "writing/loop-engineering"
---

<Bench
  rows={[
    { k: "representations", v: "512-d clap (track + section) · 384-d taste · bpm / camelot / lufs columns" },
    { k: "agents", v: "2 llm (architect, selector) — everything else deterministic" },
    { k: "critic gate", v: "≥70% harmonic · ≤6 bpm jump — set per genre · ≤4 lufs arc-rmse · 0 artist repeats — always" },
    { k: "state", v: "allin1 segments for real · genre-aware sets · 256 fast tests" },
    { k: "graded", v: "the deterministic half · the two models not yet — case set bootstrapping" }
  ]}
/>

Spotify DJ is a recommender in a costume — it legally can't touch audio, and it optimizes for everyone. This bench goes the other way: personal use, my actual files, my taste as the center of gravity, and real beatmatched renders that need the raw decodable audio streaming services can never give an algorithm.

Here's the whole idea in one run — watch the booth think:

<SetConsole />

<SystemDeepDive
  title="DJ Agent"
  inspect="dj-agent"
  subtitle="Semantics live in vectors, mixing math lives in columns — and the only two LLM components are wrapped in a deterministic verify loop."
  nodes={[
    { id: "ingest", label: "The ingest transaction", sub: "folder or link → one row", col: 0, row: 0 },
    { id: "seg", label: "Segment & analyze", sub: "allin1 cli → librosa fallback", col: 1, row: 0 },
    { id: "store", label: "Postgres + pgvector", sub: "tracks · sections · 2 spaces", col: 2, row: 0 },
    { id: "taste", label: "The taste layer", sub: "notes → 384-d · propagate", col: 3, row: 0 },
    { id: "prof", label: "Genre profiles", sub: "one table, many readers", col: 0, row: 1, accent: true },
    { id: "arch", label: "Architect", sub: "llm · brief → arc", col: 1, row: 1 },
    { id: "sel", label: "Selector", sub: "llm · generate→verify→revise", col: 2, row: 1 },
    { id: "critic", label: "Critic", sub: "verifies on the genre's gates", col: 3, row: 1, accent: true },
    { id: "hitl", label: "HITL gate", sub: "plan approved as data — auto-approved today", col: 4, row: 1, accent: true },
    { id: "core", label: "LLM core", sub: "llm.py · tiers, trace, retries", col: 1, row: 2 },
    { id: "verify", label: "Verification", sub: "gate.sh · scorecard · history", col: 3, row: 2 },
    { id: "exp", label: "Export & render", sub: "xml · m3u8 · sheet · .wav", col: 4, row: 2 }
  ]}
  edges={[
    { from: "ingest", to: "seg", label: "each track" },
    { from: "seg", to: "store", label: "one upsert" },
    { from: "taste", to: "store", label: "384-d vectors" },
    { from: "store", to: "sel", label: "blended pool" },
    { from: "prof", to: "arch", label: "default band · shape" },
    { from: "prof", to: "critic", label: "thresholds" },
    { from: "prof", to: "exp", label: "blend · stretch caps" },
    { from: "arch", to: "sel", label: "arc" },
    { from: "sel", to: "critic", label: "set plan" },
    { from: "critic", to: "sel", label: "notes → revise ≤ 3", dashed: true },
    { from: "critic", to: "hitl", label: "pass · best-so-far" },
    { from: "critic", to: "verify", label: "reused as scorecard" },
    { from: "hitl", to: "verify", label: "plan + verdict logged" },
    { from: "hitl", to: "exp", label: "approve · 3 files" },
    { from: "core", to: "arch", label: "complete()" },
    { from: "core", to: "sel", label: "complete()" }
  ]}
  stateMachines={[
    {
      title: "Set generation",
      caption: "two llms, two gates, three artifacts — audio only on demand",
      states: [
        { id: "brief", label: "vibe brief", col: 0, row: 0 },
        { id: "prof", label: "genre profile", col: 1, row: 0 },
        { id: "arc", label: "arc", col: 2, row: 0 },
        { id: "pool", label: "candidate pool", col: 3, row: 0 },
        { id: "propose", label: "propose set", col: 4, row: 0 },
        { id: "critic", label: "critic", col: 5, row: 0, kind: "gate" },
        { id: "hitl", label: "hitl approve", col: 6, row: 0, kind: "gate" },
        { id: "exp", label: "xml · m3u8 · sheet", col: 7, row: 0, kind: "terminal" },
        { id: "render", label: "render", col: 6, row: 1 },
        { id: "wav", label: "mix .wav", col: 7, row: 1, kind: "terminal" }
      ],
      transitions: [
        { from: "brief", to: "prof", label: "keyword · --genre" },
        { from: "prof", to: "arc", label: "architect" },
        { from: "arc", to: "pool", label: "bpm band" },
        { from: "pool", to: "propose", label: "selector" },
        { from: "propose", to: "critic", label: "set plan" },
        { from: "critic", to: "propose", label: "notes → revise", dashed: true },
        { from: "critic", to: "hitl", label: "genre's gates / budget" },
        { from: "hitl", to: "propose", label: "nudge", dashed: true },
        { from: "hitl", to: "exp", label: "approve" },
        { from: "exp", to: "render", label: "--render" },
        { from: "render", to: "wav", label: "stretch · crossfade · eq" }
      ]
    },
    {
      title: "Taste capture",
      caption: "reviews park until the file exists; ambiguity never guesses",
      states: [
        { id: "review", label: "review in the moment", col: 0, row: 0 },
        { id: "parked", label: "pending_taste", col: 1, row: 0 },
        { id: "match", label: "match at ingest", col: 2, row: 0, kind: "gate" },
        { id: "applied", label: "applied", col: 3, row: 0, kind: "terminal" },
        { id: "manual", label: "manual resolve", col: 3, row: 1, kind: "terminal" }
      ],
      transitions: [
        { from: "review", to: "parked", label: "no file yet" },
        { from: "parked", to: "match", label: "curator ingests" },
        { from: "match", to: "applied", label: "isrc / name+duration" },
        { from: "match", to: "manual", label: "ambiguous" }
      ]
    }
  ]}
  loops={[
    { name: "architect", model: "tier-routed", shape: "single-shot", does: "vibe brief → arc control points; deterministic shape as the no-key fallback" },
    { name: "selector", model: "tier-routed", shape: "verify loop", does: "ordered set + the right section per slot, revised against critic notes; greedy nearest-fit fallback doubles as the eval baseline" }
  ]}
  core={[
    { name: "critic thresholds", rule: "≥ 70% camelot-compatible · ≤ 6 bpm max jump — set per genre · ≤ 4 LUFS arc-RMSE · 0 back-to-back artists — critic-wide" },
    { name: "genre spread", rule: "hip-hop tolerates 12-bpm jumps, 40% harmonic, 2-bar cuts · house demands ≤ 4 bpm, 80% harmonic, allows 16-bar blends" },
    { name: "camelot wheel", rule: "the legality test is fixed math; how many clashes a set may carry is the genre's call" },
    { name: "crossfade math", rule: "half the outgoing section's bars, quantized down to a power of two (1–32), capped by the genre's blend limit" },
    { name: "greedy fallback cost", rule: "1.0·|bpm−target| + 1.5·|lufs−target| − 8.0·taste + 25 key-clash + 15 same-artist" },
    { name: "ingest transaction", rule: "one tracks row + N sections rows, ON CONFLICT(path) replaces sections — safe to re-run forever" },
    { name: "blend weights", rule: "score = 0.5·acoustic + 0.4·taste·confidence + 0.1·rating — shifts taste-heavy as labels accumulate" }
  ]}
  layers={[
    { name: "Ingest", note: "The Curator fans each file into four deterministic analyzers — sections, BPM/Camelot/LUFS, CLAP embeddings, tags — and writes one track row plus N section rows in a single idempotent transaction. Sectioning now actually runs allin1, out-of-process with a memory-linear attention patch; librosa is finally just the fallback." },
    { name: "Taste", note: "My notes become 384-d vectors; ~150 hand tags propagate to the rest of the library via CLAP neighbors with stored confidence. Blend: α 0.5 acoustic · β 0.4 taste · γ 0.1 rating. An active-learning queue surfaces what to tag next." },
    { name: "Planning", note: "A genre profile — detected from the brief or forced with --genre — sets every gate before a model runs. The Architect turns the brief into arc control points; the Selector orders tracks and plans an entry → core → exit span per slot, looping against the Critic until it passes. No key? Deterministic arc + greedy nearest-fit still ship a set — and double as the eval baseline." },
    { name: "Render", note: "Crossfade length derives from the outgoing section's bar grid — half the section, quantized down to a power of two, capped by the genre. Tempo bend is bounded by the genre's stretch limit, then equal-power blend with an EQ swap, peak-normalize. Approval always writes rekordbox.xml, .m3u8 and a printable set sheet; the .wav renders on demand." }
  ]}
  invariants={[
    "Everything is deterministic except the Architect and the Selector — genre detection is keyword matching, deliberately not a model.",
    "Transition tolerances are genre data, not code — the same set can pass as hip-hop and fail as house.",
    "The human approves the plan as data before any compute is spent on audio.",
    "Semantics live in vectors; mixing math lives in columns. Never mixed.",
    "Agents ask for a tier, never a model id — llm.py owns routing, caching, retries."
  ]}
/>

<Update date="2026.07.26" title="An audit, not a release: the acceptance metric was a constant">

No code moved in six weeks. What moved is what I know about the code — a fresh-context critic read the repo against my eval standard and filed the verdict (`docs/evals/2026-07-25-methodology-audit.md`). It found the deterministic half in good shape and the model half entirely ungraded, and the sharpest finding takes out the number the project has been steering by since June:

- **Set acceptance was 100% by construction.** The build plan named set-acceptance rate — "% of proposed sets I approve at the HITL gate" — as the number that says whether this is working. But the skill that actually runs a set mandates `HITL_LEVEL=none`, `hitl.confirm` then returns `True` without asking, and that `True` gets written into every history record. The flag can never say no, so it carries no information — and the exports (rekordbox XML, .m3u8, set sheet) get written on every run, before any human has said yes. The human-approves-the-plan rule holds in the architecture and is bypassed by the thing that invokes it.
- **Both models fail silently.** `architect.plan_arc` catches every exception and passes; the Selector's loop catches and breaks. A live model that times out, returns garbage, or gets talked out of its instructions degrades to the deterministic fallback with no log and no counter. The offline greedy path is the well-tested one; the live path is what ships by default.
- **The thing called an eval grades the wrong pipeline.** `compare_to_acoustic_baseline` defaults to no model, and its only caller never overrides that — so the A/B meant to protect the Selector never invokes it. The runner then returns 0 regardless of the result, so its exit code gates nothing.
- **Track titles reach the prompt as-is.** `selector._format_request` interpolates each candidate's title and artist straight into the model's context, and those strings come from YouTube via yt-dlp. A track named to look like an instruction is a live path into set selection. Untested.

What the audit says to keep is the part ADR 0006 got right on purpose: a key clash is a fact, not an opinion, so the verifier is math and never a model. The plan builds the case set *around* the Critic — the approval bug written as a failing case before it's fixed, then an offline tier that gates and a live tier that's allowed to fail while it's still a trend line.

</Update>

<Update date="2026.06.11" title="allin1 runs for real; the critic learns genre">

Two ADRs landed in one day, and both close gaps between what the architecture claimed and what actually executed:

- **allin1, finally (ADR 0012).** The "allin1 → librosa fallback" chain had a secret: allin1 had never run. Dependency pins blocked the install, and once bridged, its attention layer materialized full T×T score matrices on CPU — 11.3 GB peak for a 30-second clip; a real track OOMed the machine. The fix is two parts: the `allin1` CLI now runs as a subprocess from whatever Python can host it, JSON-cached per track, and a vendored memory-linear attention patch does the same math in O(T·k) — verified to float32 epsilon, and it restores the trained positional bias the upstream API migration silently dropped. Same clip now peaks at 2.07 GB, a full track at 5.2 GB. First real output: BPM 133 and 12 sensible sections on a bachata test track. Librosa is finally just the fallback.
- **Genre profiles as data (ADR 0013).** The critic's transition gates — BPM jump and harmonic compatibility — stopped being constants. A frozen profile table — nine genres plus an open default that is exactly the old behavior — feeds the architect, selector, critic, mixer and set sheet from one place. The same set with a 7-BPM jump and one key clash now *passes* as hip-hop and *fails* as house; that exact case is a unit test. Slots grew into entry → core → exit play spans sized to the genre's airtime window, crossfades quantize down to powers of two capped per genre, and approval writes a third artifact: a printable set sheet with per-transition bar counts and blend-style notes.

The fast suite went from 227 to 256 tests, still green offline.

</Update>

<Update date="2026.06.10" title="Rescope: the pipeline runs end-to-end">

The `clap-first-rescope` branch landed — the live pipeline now runs brief-to-render without a gap, and the test suite grew from 103 to 227 fast tests, all green without a database, a model, or audio on disk. Three ADRs went code-complete with it:

- **Link ingestion (ADR 0009).** Paste a Spotify or YouTube URL and the curator resolves metadata, pulls the audio via yt-dlp to FLAC, and feeds it the same idempotent ingest path as a local file. A bulk judge (`dj.taste.judge <url>`) scores a whole playlist against my taste vectors without downloading anything.
- **Two-form export (ADR 0010).** Every approved plan exports a `rekordbox.xml` for manual decks and an `.m3u8` always; `--render` still produces the automatic .wav. The plan is the product — the render is one of its outputs.
- **Grid truth (ADR 0011).** Real downbeat phase estimation, section bounds snapped to the beat grid, and `first_downbeat_s` exported as the TEMPO anchor — so what rekordbox shows matches what the mixer assumed.

</Update>

<Update date="2026.06.05" title="Code-complete, audio-pending">

A full review pass landed across phases 0–5: 103 fast tests green, every deterministic module covered. The best catch was real: librosa's beat tracker folds double-tempo genres — DnB, footwork — down to half-tempo, and a naive octave correction elsewhere was quietly un-folding them back. Fixed before any real audio ever got graded by it.

Next gate is contact with reality: the first full ingest of my actual library, where the `allin1` → librosa fallback chain finds out which segmentation backend actually installs and which genres break the beat grid.

</Update>

<Update date="2026.06.04" title="The selector earns its loop">

The Critic is the interesting half: harmonic compatibility, BPM continuity, energy-arc RMSE, artist spacing — pure math, reused unchanged as the eval scorecard. The Selector revises against its notes until pass or budget (three revisions, then best-so-far wins). The console at the top of this bench plays that exact loop. With no API key the whole pipeline degrades to a deterministic arc and greedy nearest-fit ordering, which is also the A/B baseline the agents have to beat.

</Update>

<Update date="2026.06.03" title="Capture taste before you own the file">

The strongest taste signal happens while *listening* — usually on Spotify, where there's no file to tag. So capture decouples from ingest: a review left in the moment ("Bicep — Glue: dreamy 3am closer, 5") parks in a `pending_taste` table and auto-applies when the Curator ingests a confident match — ISRC, or name plus duration. Anything ambiguous surfaces for a one-line manual resolve instead of guessing.

That last part is the design: a wrong auto-match would poison the taste layer silently, and the taste layer is the only thing here that can't be recomputed from the audio.

</Update>

<Update date="2026.06.01" title="Bench assembled — two vectors, one row">

"Sounds similar" and "is my kind of track" are different questions, so every track carries both: a 512-d CLAP acoustic vector and a 384-d taste vector embedded from my own notes — separate columns on the same Postgres row, blendable per query. "Surprise me" leans acoustic; "my favorites" leans taste. Sections are first-class rows with their own vectors, because a set doesn't mix *tracks*, it mixes the right *parts* of tracks on musical boundaries.

</Update>

<OpenQuestions
  items={[
    "Does taste propagation hold at ~150 hand tags, or does it quietly need 2,000?",
    "Genre detection is first-keyword matching — good enough for one-genre briefs, but what should ‘melodic techno into trance’ do?",
    "Phase-locked downbeats at the splice: seams currently sit within ~1.5 BPM of each other — is that audible on a real floor or only in a DAW?",
    "Plan approval was meant to be the acceptance metric, but every run auto-approves, so the flag has never carried a signal. Fix the record first — then the real question, whether approving a set as data predicts liking it as audio, is finally askable.",
    "Both models degrade to the deterministic fallback without saying so. How often does that happen on a live run? Nothing counts it today, so a set that looks fine may never have involved a model at all."
  ]}
/>
