---
title: "The Method"
summary: "I appear exactly twice per task: correcting a three-line contract before any code exists, and judging an evidence packet at the end. Everything between — implementation, verification, adversarial review — runs without me, against gates that run again at 06:17 while I sleep. This page is the loop as it actually runs, down to the files that enforce it."
thesis: "The way we build is as important as what we build."
status: "ALWAYS ON"
metrics:
  - k: "skills, one ladder"
    v: "22"
  - k: "hooks on the rim, fail-open"
    v: "5"
  - k: "repos gated on every commit + nightly"
    v: "5"
  - k: "offline eval cases, nightly at $0"
    v: "98"
  - k: "launchd agents running the loop"
    v: "4"
  - k: "token-accounting lenses"
    v: "6"
sections:
  - { id: "night", title: "While I slept" }
  - { id: "contract", title: "The contract" }
  - { id: "loop", title: "You appear exactly twice" }
  - { id: "evidence", title: "Evidence, not vibes" }
  - { id: "evals", title: "Evals from reality" }
  - { id: "loops", title: "Loop engineering" }
  - { id: "flywheel", title: "The self-improving system" }
  - { id: "machine", title: "The machine" }
---

<Section id="night" n="00" title="While I slept">

The day starts before I do. Four launchd agents run the outer loop overnight — no server, no cloud, just this machine keeping its own house across five gated repos.

<TerminalLog
  title="the night shift — this morning's digest, as it ran"
  lines={[
    { t: "06:17", k: "sys", s: "launchd: com.geoandr.nightly-gate-digest — every focus repo's gate.sh + evals.sh, caffeinated" },
    { t: "06:17", k: "out", s: "jim-agent — pass · evals — ✅ pass (2s)" },
    { t: "06:17", k: "out", s: "grocery-buddy — ✅ pass (7s)" },
    { t: "06:17", k: "out", s: "dj-agent — pass · M-Clone — pass · M-Clone evals — ✅ pass" },
    { t: "06:45", k: "sys", s: "launchd: me.ops-report — the fleet report measures, sanitizes, commits, pushes; the site redeploys" },
    { t: "07:00", k: "sys", s: "launchd: me.library-sync — the mirrored docs re-sync into the archive, deterministic" },
    { t: "07:15", k: "sys", s: "sundays: me.curator — every entry drift-checked against its repo; publishes only through its own gates" },
  ]}
/>

Drift gets caught within 24 hours instead of at the worst possible moment — including the outer loop's own drift. When idle sleep quietly inflated every gate's wall-clock, the digest's dated trend line turned "9 seconds" into "5,589 seconds" in plain sight; the fix (the run now caffeinates itself) is permanent, and the incident is exactly what the machinery exists to catch. The first session of the day opens with the digest already in hand — a red gate is the first task, green means start from the handoff.

</Section>

<Section id="contract" n="01" title="The contract">

Ceremony matches stakes. Every piece of work enters through a door on one ladder — and every nontrivial door opens onto the same three lines.

<Ladder
  title="which door, when"
  caption="every rung below the first is a real skill on this machine — click the harness below to read them"
  rungs={[
    { skill: "just ask", when: "trivial — I know exactly what I want", gets: "no ceremony" },
    { skill: "/spike", when: "prototype — can this even work?", gets: "one approach, a flat checklist, kill criteria" },
    { skill: "/plan", when: "a real feature in an existing project", gets: "a spec: contract on top, phases below" },
    { skill: "/challenge", when: "a decision worth attacking before it ships", gets: "an adversarial critic's ranked case against" },
    { skill: "/deep-plan · /deep-challenge", when: "architecture on the line — expensive to be wrong", gets: "scripted fan-out: recon, competing designs, a five-lens panel" },
    { skill: "/new-agent", when: "a brand-new agent project", gets: "the whole convention set — first question: what's the gate?" },
    { skill: "/hack", when: "a hackathon clock is running", gets: "demo-first scaffold + a T-minus schedule" },
  ]}
/>

The three lines are drafted by the agent from my messy brief, and I correct them at plan approval — that two-minute pass is where misalignment dies. From this page's own build, verbatim from its spec:

<TerminalLog
  title="a real contract — this revamp's, from docs/specs/"
  lines={[
    { k: "sys", s: "Outcome: the site reflects the current agentic-development doctrine — /method rewritten around the contract → evidence-packet loop; essays replaced; the flagship dossier wired into the fleet" },
    { k: "sys", s: "Non-goals: no visual redesign; no hand-edited fleet snapshot; no merge to main" },
    { k: "sys", s: "Acceptance evidence: content check green · library sync --check clean · lint + build green · screenshots · a grep proving zero dangling references" },
  ]}
/>

The approved plan is a **spec, and the spec is a file** — saved into the repo's `docs/specs/`, committed with the work, updated in-diff when implementation teaches us something. Iterations and reviews read the file, never the chat's memory of it. And when a spec spans modules, it leads with a diagram reviewed before the prose: structural wrongness is visible in boxes and arrows in seconds and invisible in paragraphs for pages. The PR body carries the same diagram, so merge review starts at the altitude plan review did.

</Section>

<Section id="loop" n="02" title="You appear exactly twice">

Between the two touchpoints, the lane runs itself: implement single-lane, self-verify against the repo's own gates and suites, survive a fresh-context adversarial critic, and end at an evidence packet whose last line proposes the next contract — so accepting evidence and approving the next spec is one touch.

<ArchitectureDiagram
  title="the task loop"
  caption="accent outline = a deterministic or human gate; the model never grades its own work"
  nodes={[
    { id: "brief", label: "Messy brief", sub: "two sentences from me", col: 0, row: 0 },
    { id: "spec", label: "The spec", sub: "contract + phases · docs/specs/", col: 1, row: 0 },
    { id: "approve", label: "Touchpoint 1", sub: "correct + approve", col: 2, row: 0, accent: true },
    { id: "lane", label: "The lane", sub: "implement + self-verify", col: 3, row: 0 },
    { id: "critic", label: "The critic", sub: "fresh context · told to refute", col: 3, row: 1, accent: true },
    { id: "packet", label: "Evidence packet", sub: "checks · eval delta · NOT verified", col: 2, row: 1 },
    { id: "accept", label: "Touchpoint 2", sub: "accept ≈ approve the next spec", col: 1, row: 1, accent: true },
  ]}
  edges={[
    { from: "brief", to: "spec", label: "agent drafts" },
    { from: "spec", to: "approve" },
    { from: "approve", to: "lane", label: "runs without me" },
    { from: "lane", to: "critic", label: "diff + spec only" },
    { from: "critic", to: "packet", label: "verdict, in the packet" },
    { from: "packet", to: "accept" },
    { from: "accept", to: "spec", label: "the proposed next contract", dashed: true },
  ]}
/>

<LaneBoard
  title="the board while this page was rebuilt"
  caption="the session that produced the page you're reading — reads fanned out, writes stayed single-lane"
  lanes={[
    {
      name: "conductor",
      role: "conductor",
      sub: "this session",
      lines: [
        { k: "cmd", s: "revamp the site around the current doctrine" },
        { k: "sys", s: "2 explorers fan out — the site · the harness docs; a planner drafts the mechanics" },
        { k: "out", s: "spec corrected twice against a moving main, then approved + committed" },
        { k: "out", s: "implementation runs here, single-lane, gate on every commit" },
      ],
    },
    {
      name: "lane: exploration",
      role: "executor",
      sub: "read-only subagents · own contexts",
      lines: [
        { k: "sys", s: "map the routes, the content graph, the fleet wiring — conclusions only" },
        { k: "out", s: "~150k tokens of reading that never touched the main window" },
      ],
    },
  ]}
  contract={[
    { name: "outcome", detail: "the observable result — if I can't write it, the work isn't ready to delegate" },
    { name: "non-goals", detail: "what not to expand into, said out loud" },
    { name: "acceptance evidence", detail: "checks the agent can run — eval cases wearing a different hat" },
    { name: "verify", detail: "the repo's own gate and suites — the lane proves itself before claiming done" },
    { name: "stop", detail: "an evidence packet, a blocked question, or a spent retry budget — never 'probably right'" },
  ]}
/>

**Fan out for reads, single-lane for writes.** Parallel subagents investigate and review; implementation writes in one lane with checkpoints, unless workstreams touch disjoint modules *and* disjoint runtimes — worktrees isolate files, not simulators, ports, or datastores.

</Section>

<Section id="evidence" n="03" title="Evidence, not vibes">

"Done, please test it" is a malformed result. Done is a packet: the spec cited; each acceptance check with the actual command and output; the **eval delta** — cases passing before vs. after, added, retired; the critic's verdict; screenshots for anything with a UI; and the honest **NOT verified** list. That last line is the most important one — and gaps never close silently: every NOT-verified item either blocks acceptance or gets an explicit "accepted without X" plus a tracked follow-up.

<ArchitectureDiagram
  title="what a commit has to survive"
  caption="one file per repo, enforced at the commit and re-run nightly — including the anti-cheat lines"
  nodes={[
    { id: "diff", label: "The diff", sub: "any lane · any repo", col: 0, row: 0 },
    { id: "commit", label: "git commit", sub: "any session, every time", col: 1, row: 0 },
    { id: "hook", label: "guard-commit", sub: "global hook · fail-open", col: 2, row: 0, accent: true },
    { id: "gate", label: "gate.sh", sub: "lint + hermetic suite + anti-cheat · <120s", col: 3, row: 0, accent: true },
    { id: "green", label: "committed", sub: "green that means something", col: 4, row: 0 },
    { id: "blocked", label: "blocked", sub: "the exact failing check, returned", col: 3, row: 1 },
    { id: "night", label: "night watch", sub: "same file · 06:17 · digested", col: 4, row: 1 },
  ]}
  edges={[
    { from: "diff", to: "commit" },
    { from: "commit", to: "hook", label: "PreToolUse" },
    { from: "hook", to: "gate", label: "runs the repo's own gate" },
    { from: "gate", to: "green", label: "exit 0" },
    { from: "gate", to: "blocked", label: "red", dashed: true },
    { from: "gate", to: "night", label: "runs again nightly" },
  ]}
/>

The anti-cheat lines exist because models game checks: in one vendor's audit, sealing git history and network access dropped a model's benchmark score from 87.1% to 73.0% — 63% of "passing" runs had cheated. The tells are deterministic to check, so they live in the gate, not in my vigilance: a diff that deletes test assertions or adds skip markers goes red and names the file.

Review depth then tracks **consequence, not habit**: anything touching money or acting externally gets line-level review with the packet as context; product logic behind a gate and evals gets packet-only review; spikes and scaffolding get one look at the outcome. The tiers are what machine-absorbed verification buys — judgment lands on architecture, tradeoffs, and intent instead of on re-testing what a machine already proved.

</Section>

<Section id="evals" n="04" title="Evals from reality">

Tests check code paths; evals judge behavior across cases — did the agent make the right decision given this input, this history, this ledger? Anything with an LLM decision carries a scored suite, and suites grow one way only: **every real bug becomes a case before its fix** — written first, watched failing, then fixed in the same diff. jim replays 98 offline cases nightly for $0.00 with a baseline; the flagship carries a whole eval *product* — versioned corpus packs, preregistered experiments, and an 81-case holdout sealed behind a hashing workflow so no tuning session can quietly train on the exam.

Graders are picked by cost curve: deterministic code-graders (free forever), then snapshot-replay, then — last resort — a pinned, calibrated LLM judge with an "Unknown" escape. And the ruler itself gets audited, because a metric once read a perfect score while the output dropped the exact figure the feature existed to surface: score predicates stay byte-identical to production, harness prompts are parity-fenced against app prompts, and shipping-gate suites earn a periodic every-row read.

<Ref to="projects/m-clone#eval-product" note="the eval product in full: corpus packs, sealed holdout, per-row adjudication" />

<Ref to="projects/jim" note="98 cases, zero credentials, every night" />

</Section>

<Section id="loops" n="05" title="Loop engineering">

The highest-leverage question is asked before any task starts: **can the agent verify this without me?** If no — no test, no eval case, no seeded data — the cheapest missing verifier gets built as part of the task. Everything else in the inner loop follows the same logic:

**Context in by machinery.** The session-context hook injects git state, the overnight digest, and the last handoff before I type a word. State lives on disk, so any lane is restartable — and the context window never becomes the only copy of anything.

**Stop conditions are explicit.** Two iterations on the same problem with no genuinely new hypothesis means stop: get a fresh-context review — a critic reading only the repo and the spec — instead of a third argument in the same chat. The session that produced the confusion is the worst place to resolve it.

**Verification paces itself.** Long verification — device eval runs, full simulator suites — runs as background work with scheduled wakeups; the lane sleeps between rounds instead of burning context polling. One recent feature chained five eval rounds this way and correctly did nothing when a stale wakeup fired after the work was done.

**Models are tiered by judgment.** The top tier plans, reviews evidence, and judges evals; well-contracted execution goes to cheaper, faster tiers. The contract is what makes this safe — a task specified to runnable-acceptance-check precision doesn't need the smartest model, it needs an obedient one.

</Section>

<Section id="flywheel" n="06" title="The self-improving system">

The system watches its own exhaust and proposes its own fixes; I stay the approval gate. Exhaust accumulates automatically — nightly digests, a babysit log with one line per moment I had to do something by hand, a handoff per session. A proposer skill mines the accumulation, clusters recurring friction, and files proposals it cannot apply: propose-only, by construction.

<ArchitectureDiagram
  title="the flywheel — friction leaves as an artifact"
  caption="every proposal names the cheapest durable fix; I approve or reject in minutes"
  nodes={[
    { id: "exhaust", label: "Exhaust", sub: "digests · babysit log · handoffs", col: 0, row: 0 },
    { id: "proposer", label: "The proposer", sub: "clusters friction · propose-only", col: 1, row: 0, accent: true },
    { id: "ladder", label: "Cheapest fix wins", sub: "hook < gate line < instruction < skill < eval case", col: 2, row: 0, accent: true },
    { id: "approve", label: "Human approval", sub: "minutes, not meetings", col: 3, row: 0, accent: true },
    { id: "harness", label: "The harness", sub: "compounds — felt once", col: 4, row: 0 },
  ]}
  edges={[
    { from: "exhaust", to: "proposer", label: "read weekly" },
    { from: "proposer", to: "ladder", label: "one fix per cluster" },
    { from: "ladder", to: "approve" },
    { from: "approve", to: "harness", label: "as contracted work" },
    { from: "harness", to: "exhaust", label: "next week's friction, smaller", dashed: true },
  ]}
/>

Instruction files ride the same loop, treated as reactive failure logs: a line lands in a repo's `AGENTS.md` because an agent actually misbehaved, and a line that stops paying gets deleted. Every re-explanation in a session is a bug in that file, fixed with one line, then — not someday.

The proof is this site. It watches the fleet's transcripts, reads the night's digests, **files its own report every morning**, and syncs its entries against their source repos weekly — gated by its own content check and build rather than by my attention. The archive maintains itself and shows its work in <a href="/v2/ops">the ops room</a>.

</Section>

<Section id="machine" n="07" title="The machine">

The design rule underneath all of it: the main context window is the most expensive real estate in the system, so heavy reading happens in someone else's window and only conclusions come back. Below is the machine itself — every layer clickable down to the file that enforces it.

<SystemDeepDive
  title="The Harness"
  inspect="method"
  subtitle="Claude Code as the engine; the harness as transmission, brakes, and instrumentation. Everything below is real configuration, quoted from the files that enforce it."
  nodes={[
    { id: "contract", label: "CLAUDE.md", sub: "contract-first · disagree out loud", col: 0, row: 0, accent: true },
    { id: "cc", label: "Claude Code", sub: "the main loop", col: 1, row: 0 },
    { id: "skills", label: "The skills", sub: "22 front doors · light → deep", col: 2, row: 0 },
    { id: "specialists", label: "Specialists", sub: "critic · researcher · explore fleets", col: 0, row: 1 },
    { id: "rim", label: "Hook rim", sub: "5 guards · deterministic · fail-open", col: 1, row: 1, accent: true },
    { id: "memory", label: "Memory & handoffs", sub: "state survives on disk", col: 2, row: 1 },
    { id: "meter", label: "Token meter", sub: "6 lenses + a ledger", col: 0, row: 2 },
    { id: "fleet", label: "Fleet gates", sub: "gate.sh + evals.sh × 5 repos", col: 1, row: 2, accent: true },
    { id: "night", label: "Night automation", sub: "06:17 · 06:45 · 07:00 · sun 07:15", col: 2, row: 2 },
  ]}
  edges={[
    { from: "contract", to: "cc", label: "governs every turn" },
    { from: "skills", to: "cc", label: "front doors" },
    { from: "specialists", to: "cc", label: "conclusions only" },
    { from: "cc", to: "rim", label: "every tool call" },
    { from: "cc", to: "memory", label: "handoff · recall" },
    { from: "cc", to: "meter", label: "every window priced" },
    { from: "rim", to: "fleet", label: "guard-commit runs the gate" },
    { from: "fleet", to: "night", label: "re-run nightly" },
    { from: "night", to: "cc", label: "the morning banner" },
  ]}
  stateMachines={[
    {
      title: "A session",
      caption: "state survives on disk, not in my head",
      states: [
        { id: "boot", label: "session start", col: 0, row: 0 },
        { id: "inject", label: "context hook", col: 1, row: 0, kind: "gate" },
        { id: "onboard", label: "/onboard", col: 2, row: 0, kind: "gate" },
        { id: "work", label: "the loop", col: 3, row: 0 },
        { id: "wrap", label: "/end-session", col: 4, row: 0, kind: "gate" },
        { id: "handoff", label: "handoff on disk", col: 5, row: 0, kind: "terminal" },
      ],
      transitions: [
        { from: "boot", to: "inject", label: "git + digest + last handoff" },
        { from: "inject", to: "onboard", label: "lean prefix" },
        { from: "onboard", to: "work", label: "drift flagged first" },
        { from: "work", to: "wrap", label: "docs then handoff" },
        { from: "wrap", to: "handoff", label: "next steps, ordered" },
      ],
    },
    {
      title: "A task",
      caption: "two human touchpoints; everything else is the lane's job",
      states: [
        { id: "brief", label: "messy brief", col: 0, row: 0 },
        { id: "spec", label: "spec drafted", col: 1, row: 0 },
        { id: "approve", label: "correct + approve", col: 2, row: 0, kind: "gate" },
        { id: "lane", label: "implement + self-verify", col: 3, row: 0 },
        { id: "critic", label: "adversarial critic", col: 4, row: 0, kind: "gate" },
        { id: "packet", label: "evidence packet", col: 5, row: 0, kind: "gate" },
        { id: "next", label: "next contract", col: 6, row: 0, kind: "terminal" },
      ],
      transitions: [
        { from: "brief", to: "spec", label: "three lines first" },
        { from: "spec", to: "approve", label: "touchpoint 1" },
        { from: "approve", to: "lane", label: "runs without me" },
        { from: "lane", to: "critic", label: "diff + spec only" },
        { from: "critic", to: "packet", label: "verdict included" },
        { from: "packet", to: "next", label: "touchpoint 2 — accept ≈ approve" },
      ],
    },
  ]}
  loops={[
    { name: "critic", model: "opus · xhigh", shape: "single-shot", does: "the strongest honest case against — read-only, so the cost of being wrong stays a paragraph" },
    { name: "researcher", model: "sonnet · max", shape: "fan 1–3", does: "current sources, triangulated, ~18-month staleness flags; returns conclusions, never raw pages" },
    { name: "explore fleet", model: "dial: haiku→opus", shape: "4 / 6 / 8 parallel", does: "read-only codebase recon for /plan and /deep-plan — the map comes back, the territory stays out" },
    { name: "critic panel", model: "sonnet ×5 lenses", shape: "1–3 per lens", does: "correctness · failure modes · security · scale · simpler-alternatives, run blind and deduped" },
    { name: "design trio", model: "sonnet ×3 stances", shape: "judged", does: "MVP-first vs risk-first vs robustness-first; a judge scores, opus grafts the best of the losers" },
  ]}
  core={[
    { name: "the contract", rule: "every nontrivial task opens with three lines — outcome, non-goals, runnable acceptance evidence — corrected by a human at plan approval" },
    { name: "the spec is a file", rule: "the approved plan lands in docs/specs/ and ships with the diff; iterations read the file, never the chat's memory of it" },
    { name: "evidence packet", rule: "done = spec cited, checks with actual output, eval delta, critic verdict, NOT-verified list, proposed next contract — gaps never close silently" },
    { name: "bugs become cases", rule: "a real failure is written into the eval suite and watched failing before it's fixed — same diff, no silent patching" },
    { name: "anti-cheat", rule: "a diff with net-negative test assertions or skip/xfail markers turns the gate red and names the file" },
    { name: "commit gate", rule: "git commit runs the repo's own gate.sh and blocks on red; --no-verify is the deliberate escape hatch, said out loud in the message" },
    { name: "two iterations", rule: "two failed attempts with no new hypothesis → a fresh-context review reads only repo + spec; never a third argument in the same chat" },
    { name: "fan-out", rule: "subagents fan out for reads and review; writes run single-lane — worktrees isolate files, not simulators, ports, or datastores" },
    { name: "guard-bash", rule: "recursive deletes are blocked when the target resolves to a protected directory or its ancestor; fails open" },
    { name: "guard-secrets", rule: "high-confidence keys never land in tracked files; .env, examples, and key files are exempt by design" },
    { name: "plan rules", rule: "no time estimates, ever; phases end exactly where a human verifies; total phases stay in the single digits" },
    { name: "the meter's honesty", rule: "dollar figures are API-list-price equivalents, not the bill; input counts under-report — trust output and cache-read" },
  ]}
  layers={[
    { name: "Contract", note: "One global CLAUDE.md: treat my framing as a hypothesis, pressure-test consequential decisions, disagree out loud, resolve trivial choices alone. It makes the default posture adversarial instead of agreeable — and it's where the task loop's rules are canonical." },
    { name: "Skills", note: "Twenty-two front doors on one ladder: session bookends, the shaping tier (/spike, /plan, /challenge), scripted deep siblings, project starters (/new-agent, /hack), the proposer and /retro, and six token lenses. One shape throughout: frame a self-contained brief, delegate the heavy work, synthesize back." },
    { name: "Specialists", note: "A critic paid to attack — self-invoked on high-stakes specs, mandatory before every evidence packet. A researcher paid to read elsewhere. Explore fleets for recon. All return conclusions into the main loop — never their raw material." },
    { name: "Rim", note: "Five hooks wrap every session: catastrophic-command guard, commit gate, secret-write guard, format-on-edit, session-context injection. Deterministic shell, zero model, all fail-open." },
    { name: "Memory & meter", note: "Handoffs make state survive sessions; memory files make taste survive projects; the token ledger makes cost a number instead of a feeling." },
    { name: "Fleet", note: "The layer that runs without me: five focus repos each carry an executable gate.sh (guard-commit blocks red commits) and, where an LLM decides anything, an offline evals.sh; a 06:17 LaunchAgent runs both across the fleet and digests the result; the city board reads that digest, files its own morning report, and curates its own entries — deterministically gated." },
  ]}
  invariants={[
    "Raw research never enters the main window — subagents read; the loop receives conclusions.",
    "Disagreement is contractual: my framing is a hypothesis, and the critic is paid to attack it.",
    "A task is done when its evidence packet says so — and the NOT-verified list is the packet's most important line.",
    "Bugs become eval cases before fixes, in the same diff.",
    "Hooks fail open — they guard accidents, not deliberate obfuscation.",
    "Every token in every window is ledgered, with drill-down to what each subagent was fed.",
    "Verification is agent-runnable: gates and evals pass or fail without me, on every commit and every night.",
    "The board and I read the same digest — the site is the instrument panel, not a brochure.",
  ]}
/>

---

The harness is why a one-person shop ships an on-device banking copilot with a sealed-holdout eval product, and agents on payment rails with red-team suites. Same nervous system as the products, pointed at the producer — and increasingly, running without him.

<Ref to="projects/m-clone" note="the doctrine's flagship receipt — an eval product wearing an app" />

<Ref to="writing/you-appear-exactly-twice" note="the loop, argued in full" />

</Section>
