---
title: "The Method"
summary: "I don't type code into a box; I operate a small fleet. One conductor session writes briefs and reviews diffs; executors run in their own windows; every repo verifies itself on every commit; and the checks run again at 06:17 while I sleep. This page is the day as it actually runs — down to the files that enforce it."
thesis: "The way we build is as important as what we build."
status: "ALWAYS ON"
metrics:
  - k: "skills, one ladder"
    v: "21"
  - k: "hooks on the rim, fail-open"
    v: "5"
  - k: "repos gated on every commit + nightly"
    v: "4"
  - k: "offline eval cases, nightly at $0"
    v: "87"
  - k: "launchd agents running the loop"
    v: "3"
  - k: "token-accounting lenses"
    v: "6"
sections:
  - { id: "slept", title: "While I slept" }
  - { id: "open", title: "The two-minute open" }
  - { id: "ladder", title: "The ladder" }
  - { id: "lanes", title: "Conduct, don't type" }
  - { id: "verify", title: "Verification without me" }
  - { id: "flywheel", title: "The flywheel" }
  - { id: "machine", title: "The machine" }
---

<Section id="slept" n="00" title="While I slept">

The day starts before I do. Three launchd agents run the loop overnight — no server, no cloud, just this machine keeping its own house.

<TerminalLog
  title="the night shift — this morning, as it ran"
  lines={[
    { t: "06:17", k: "sys", s: "launchd: com.geoandr.nightly-gate-digest — every focus repo's gate.sh + evals.sh" },
    { t: "06:17", k: "out", s: "jim-agent — ✅ pass (9s) · evals — ✅ pass (2s)" },
    { t: "06:17", k: "out", s: "grocery-buddy — ✅ pass (3s) · procurement-agent — ✅ pass (2s)" },
    { t: "06:17", k: "out", s: "dj-agent — ✅ pass (7s)" },
    { t: "06:45", k: "sys", s: "launchd: me.ops-report — the fleet report measures, sanitizes, commits, pushes; the site redeploys" },
    { t: "07:15", k: "sys", s: "sundays: me.curator — every entry drift-checked against its repo; publishes only through its own gates" }
  ]}
/>

Drift gets caught within 24 hours instead of at the worst possible moment — and I don't have to remember to check, because the checking isn't mine anymore. The gate lines above are quoted from today's digest file; the first session of the day opens with them already in hand.

</Section>

<Section id="open" n="01" title="The two-minute open">

No dashboard, no status meeting with myself. A SessionStart hook injects three things before I type a word:

<TerminalLog
  title="a session start — the hook's injection, condensed"
  lines={[
    { k: "sys", s: "SessionStart · session-context.sh →" },
    { k: "out", s: "Git: on 'main', 0 uncommitted change(s). Last commit: 15c85a8 …" },
    { k: "out", s: "Gate digest (2026-07-06): 4 repos ✅ · evals ✅" },
    { k: "out", s: "## Next steps — the last handoff's ordered list, yesterday-me briefing today-me" }
  ]}
/>

The triage rule, in order: a red gate or a yellow eval regression **is** the first task — you don't build on a broken foundation. Green → start from the handoff's next steps. No handoff → pick a rung below.

</Section>

<Section id="ladder" n="02" title="The ladder">

Ceremony matches stakes. Every piece of work enters through one of these doors — the expensive mistake is over-planning small things and under-planning big ones.

<Ladder
  title="which door, when"
  caption="every rung below the first is a real skill on this machine — click the harness below to read them"
  rungs={[
    { skill: "just ask", when: "trivial — I know exactly what I want", gets: "no ceremony" },
    { skill: "/spike", when: "prototype — can this even work?", gets: "one approach, a flat checklist, kill criteria" },
    { skill: "/plan", when: "a real feature in an existing project", gets: "dense phases, each ending where a human verifies" },
    { skill: "/challenge", when: "a decision worth attacking before it ships", gets: "an opus critic's ranked case against" },
    { skill: "/deep-plan · /deep-challenge", when: "architecture on the line — expensive to be wrong", gets: "scripted fan-out: recon, competing designs, a five-lens panel" },
    { skill: "/new-agent", when: "a brand-new agent project", gets: "the whole convention set — first question: what's the gate?" },
    { skill: "/hack", when: "a hackathon clock is running", gets: "demo-first scaffold + a T-minus schedule" }
  ]}
/>

The deep tier is an economic decision, not a default: the skill frames a self-contained brief and a co-located script runs a bounded fan-out — a few hundred thousand tokens of parallel scrutiny on the decisions that justify it, and nothing on the ones that don't.

</Section>

<Section id="lanes" n="03" title="Conduct, don't type">

The session shape that changes throughput isn't a better prompt — it's **conduct, don't type**. One session writes briefs and reviews diffs; implementation runs in parallel lanes — background agents and git worktrees — that verify themselves against their own gates.

<LaneBoard
  title="the board while this page was rebuilt"
  caption="a real morning — the conductor and its two executor lanes, from the session that produced the page you're reading"
  lanes={[
    {
      name: "conductor",
      role: "conductor",
      sub: "this session",
      lines: [
        { k: "cmd", s: "revamp /method + the project deep dives" },
        { k: "sys", s: "3 explorers fan out — the site · jim-agent · grocery + dj" },
        { k: "out", s: "plan approved — two implementation briefs written" },
        { k: "out", s: "reviewing diffs, not token streams" }
      ]
    },
    {
      name: "lane: grocery-buddy",
      role: "executor",
      sub: "background agent · own context",
      lines: [
        { k: "sys", s: "brief: goal · constraints · files it owns · verify command · what done means" },
        { k: "out", s: "compressing the topology · drawing the real sub-graphs" },
        { k: "out", s: "verify: tsc ✅ · content check ✅" }
      ]
    },
    {
      name: "lane: dj-agent",
      role: "executor",
      sub: "background agent · own context",
      lines: [
        { k: "sys", s: "same brief shape, different territory — lanes can't stomp each other" },
        { k: "out", s: "ingest transaction · generation loop · critic thresholds" },
        { k: "out", s: "verify: tsc ✅ · content check ✅" }
      ]
    }
  ]}
  contract={[
    { name: "goal", detail: "one sentence, unambiguous — if I can't write it, the work isn't ready to delegate" },
    { name: "constraints", detail: "what must not change, said out loud" },
    { name: "files", detail: "the lane's territory, named — so lanes can't collide" },
    { name: "verify", detail: "the exact command the lane must pass — usually that repo's gate.sh" },
    { name: "done", detail: "what finished looks like, so the lane can stop itself" }
  ]}
/>

The ramp is deliberately boring: one conductor plus one executor, adding a lane only when the current count feels idle. The ceiling isn't the model — it's brief-writing and diff-review bandwidth. A lane gets touched when it pings or at its checkpoint, never watched; if lanes keep blocking on me, the fix is fewer lanes or sharper briefs.

</Section>

<Section id="verify" n="04" title="Verification without me">

The highest-leverage question in the whole system is asked *before* any task starts: **can the agent verify this without me?** If the answer is no — no test to run, no eval case, no seeded data — that gets built first. It feels slower; it's faster by mid-session, because the agent self-corrects instead of queueing on my eyeballs.

<ArchitectureDiagram
  title="what a commit has to survive"
  caption="one file per repo, three enforcement points: the commit, the night watch, and — when repos get remotes — CI runs the same script"
  nodes={[
    { id: "diff", label: "The diff", sub: "any lane · any repo", col: 0, row: 0 },
    { id: "commit", label: "git commit", sub: "any session, every time", col: 1, row: 0 },
    { id: "hook", label: "guard-commit", sub: "global hook · fail-open", col: 2, row: 0, accent: true },
    { id: "gate", label: "gate.sh", sub: "lint + hermetic suite · <120s", col: 3, row: 0, accent: true },
    { id: "green", label: "committed", sub: "lint + suite proven", col: 4, row: 0 },
    { id: "blocked", label: "blocked", sub: "the exact failing check, returned", col: 3, row: 1 },
    { id: "night", label: "night watch", sub: "same file · 06:17 · digested", col: 4, row: 1 }
  ]}
  edges={[
    { from: "diff", to: "commit" },
    { from: "commit", to: "hook", label: "PreToolUse" },
    { from: "hook", to: "gate", label: "runs the repo's own gate" },
    { from: "gate", to: "green", label: "exit 0" },
    { from: "gate", to: "blocked", label: "red", dashed: true },
    { from: "gate", to: "night", label: "runs again nightly" }
  ]}
/>

Evals are the same idea for the nondeterministic parts: anything with an LLM decision carries an offline, zero-credential `evals.sh` — JIM's replays 87 cases in about two seconds for $0.00, armed with a baseline so a regression is a verdict, not a feeling. The full harness is drawn inside <a href="/projects/jim">JIM's dossier</a>. And `--no-verify` stays available as the deliberate escape hatch — used rarely, said out loud in the commit message.

</Section>

<Section id="flywheel" n="05" title="The flywheel">

Everything above compounds through one rule and one ritual. The rule: **anything done or explained three times becomes a skill that session.** The ritual: a thirty-minute weekly retro over one question — *where did I babysit this week?*

<ArchitectureDiagram
  title="the flywheel — friction leaves as an artifact"
  caption="skip the retro for a month and the setup stays exactly this good; run it weekly and every annoyance is felt approximately once"
  nodes={[
    { id: "catch", label: "A babysit moment", sub: "re-explaining · hand-verifying", col: 0, row: 0 },
    { id: "log", label: "BABYSIT_LOG.md", sub: "one line · ten seconds", col: 1, row: 0 },
    { id: "retro", label: "/retro — weekly", sub: "conversion, not review", col: 2, row: 0, accent: true },
    { id: "fix", label: "cheapest fix wins", sub: "hook < gate < instruction < skill", col: 3, row: 0, accent: true },
    { id: "harness", label: "The harness", sub: "compounds — felt once", col: 4, row: 0 }
  ]}
  edges={[
    { from: "catch", to: "log" },
    { from: "log", to: "retro", label: "the open list" },
    { from: "retro", to: "fix", label: "one fix per item" },
    { from: "fix", to: "harness", label: "applied in-session" },
    { from: "harness", to: "catch", label: "next week's friction, smaller", dashed: true }
  ]}
/>

Instruction files ride the same loop, treated as code: one canonical `AGENTS.md` per repo, containing only what an agent gets wrong without being told. Every re-explanation in a session is a bug in that file, fixed with one line, then — not someday.

The proof is this site. It watches the fleet's transcripts, reads the night's digests, **files its own report every morning**, and syncs its entries against their source repos weekly — gated by its own content check and build rather than by my attention. The archive maintains itself and shows its work in <a href="/v2/ops">the ops room</a>.

</Section>

<Section id="machine" n="06" title="The machine">

The design rule underneath all of it: the main context window is the most expensive real estate in the system, so heavy reading happens in someone else's window and only conclusions come back. Below is the machine itself — every layer clickable down to the file that enforces it.

<SystemDeepDive
  title="The Harness"
  inspect="method"
  subtitle="Claude Code as the engine; the harness as transmission, brakes, and instrumentation. Everything below is real configuration, quoted from the files that enforce it."
  nodes={[
    { id: "contract", label: "CLAUDE.md", sub: "framing = a hypothesis · disagree out loud", col: 0, row: 0, accent: true },
    { id: "cc", label: "Claude Code", sub: "the main loop", col: 1, row: 0 },
    { id: "skills", label: "The skills", sub: "21 front doors · light → deep", col: 2, row: 0 },
    { id: "specialists", label: "Specialists", sub: "critic · researcher · explore fleets", col: 0, row: 1 },
    { id: "rim", label: "Hook rim", sub: "5 guards · deterministic · fail-open", col: 1, row: 1, accent: true },
    { id: "memory", label: "Memory & handoffs", sub: "state survives on disk", col: 2, row: 1 },
    { id: "meter", label: "Token meter", sub: "6 lenses + a ledger", col: 0, row: 2 },
    { id: "fleet", label: "Fleet gates", sub: "gate.sh + evals.sh × 4 repos", col: 1, row: 2, accent: true },
    { id: "night", label: "Night automation", sub: "06:17 · 06:45 · sun 07:15", col: 2, row: 2 }
  ]}
  edges={[
    { from: "contract", to: "cc", label: "governs every turn" },
    { from: "skills", to: "cc", label: "front doors" },
    { from: "specialists", to: "cc", label: "conclusions only" },
    { from: "cc", to: "rim", label: "every tool call" },
    { from: "cc", to: "memory", label: "handoff · recall" },
    { from: "cc", to: "meter", label: "every window priced" },
    { from: "rim", to: "fleet", label: "guard-commit runs the gate" },
    { from: "fleet", to: "night", label: "re-run nightly" },
    { from: "night", to: "cc", label: "the morning banner" }
  ]}
  stateMachines={[
    {
      title: "A session",
      caption: "state survives on disk, not in my head",
      states: [
        { id: "boot", label: "session start", col: 0, row: 0 },
        { id: "inject", label: "context hook", col: 1, row: 0, kind: "gate" },
        { id: "onboard", label: "/onboard", col: 2, row: 0, kind: "gate" },
        { id: "work", label: "the loop", col: 3, row: 0 },
        { id: "wrap", label: "/end-session", col: 4, row: 0, kind: "gate" },
        { id: "handoff", label: "handoff on disk", col: 5, row: 0, kind: "terminal" }
      ],
      transitions: [
        { from: "boot", to: "inject", label: "git + digest + last handoff" },
        { from: "inject", to: "onboard", label: "lean prefix" },
        { from: "onboard", to: "work", label: "drift flagged first" },
        { from: "work", to: "wrap", label: "docs then handoff" },
        { from: "wrap", to: "handoff", label: "next steps, ordered" }
      ]
    },
    {
      title: "A deep-skill run",
      caption: "we choose the fan; the script guarantees it",
      states: [
        { id: "brief", label: "self-contained brief", col: 0, row: 0 },
        { id: "intensity", label: "intensity dial", col: 1, row: 0 },
        { id: "fan", label: "scripted fan-out", col: 2, row: 0 },
        { id: "dedupe", label: "merge + dedupe", col: 3, row: 0 },
        { id: "opus", label: "opus synthesis", col: 4, row: 0, kind: "gate" },
        { id: "adjudicate", label: "my honest take", col: 5, row: 0, kind: "gate" },
        { id: "decision", label: "decision", col: 6, row: 0, kind: "terminal" }
      ],
      transitions: [
        { from: "brief", to: "intensity", label: "low · med · high" },
        { from: "intensity", to: "fan", label: "sonnet breadth ×N" },
        { from: "fan", to: "dedupe", label: "independent passes" },
        { from: "dedupe", to: "opus", label: "steelman + rank" },
        { from: "opus", to: "adjudicate", label: "don't just relay" },
        { from: "adjudicate", to: "decision", label: "fix · accept · reject" }
      ]
    }
  ]}
  loops={[
    { name: "critic", model: "opus · xhigh", shape: "single-shot", does: "the strongest honest case against — read-only, so the cost of being wrong stays a paragraph" },
    { name: "researcher", model: "sonnet · max", shape: "fan 1–3", does: "current sources, triangulated, ~18-month staleness flags; returns conclusions, never raw pages" },
    { name: "explore fleet", model: "dial: haiku→opus", shape: "4 / 6 / 8 parallel", does: "read-only codebase recon for /plan and /deep-plan — the map comes back, the territory stays out" },
    { name: "critic panel", model: "sonnet ×5 lenses", shape: "1–3 per lens", does: "correctness · failure modes · security · scale · simpler-alternatives, run blind and deduped" },
    { name: "design trio", model: "sonnet ×3 stances", shape: "judged", does: "MVP-first vs risk-first vs robustness-first; a judge scores, opus grafts the best of the losers" }
  ]}
  core={[
    { name: "the gate question", rule: "before any task: can the agent verify this without me? if no — no test, no eval, no seeded data — build that first" },
    { name: "commit gate", rule: "git commit runs the repo's own gate.sh and blocks on red; --no-verify is the deliberate escape hatch, said out loud in the message" },
    { name: "lane contract", rule: "a parallel lane starts with a written brief: goal, constraints, files to respect, the verify command it must pass, and what done means" },
    { name: "guard-bash", rule: "recursive deletes are blocked when the target resolves to a protected directory or its ancestor — anything strictly inside stays deletable; fails open" },
    { name: "guard-secrets", rule: "high-confidence keys never land in tracked files; .env, examples, and key files are exempt by design" },
    { name: "plan rules", rule: "no time estimates, ever; phases end exactly where a human verifies; total phases stay in the single digits" },
    { name: "doc discipline", rule: "no new document without explicit approval — update what exists, never sprawl" },
    { name: "deep-skill contract", rule: "the skill frames the brief; a script runs the fan-out — bounded, deterministic, reproducible" },
    { name: "the meter's honesty", rule: "dollar figures are API-list-price equivalents, not the bill; input counts under-report — trust output and cache-read" }
  ]}
  layers={[
    { name: "Contract", note: "One global CLAUDE.md: treat my framing as a hypothesis, pressure-test consequential decisions, disagree out loud, resolve trivial choices alone. It makes the default posture adversarial instead of agreeable." },
    { name: "Skills", note: "Twenty-one front doors on one ladder: session bookends, the shaping tier (/spike, /plan, /challenge), scripted deep siblings, project starters (/new-agent, /hack), the weekly /retro, and six token lenses. One shape throughout: frame a self-contained brief, delegate the heavy work, synthesize back." },
    { name: "Specialists", note: "A critic on Opus paid to attack, a researcher on Sonnet paid to read elsewhere, Explore fleets for recon. All of them return conclusions into the main loop — never their raw material." },
    { name: "Rim", note: "Five hooks wrap every session: catastrophic-command guard, commit gate, secret-write guard, format-on-edit, session-context injection. Deterministic shell, zero model, all fail-open." },
    { name: "Memory & meter", note: "Handoffs make state survive sessions; memory files make taste survive projects; the token ledger makes cost a number instead of a feeling." },
    { name: "Fleet", note: "The layer that runs without me: every focus repo carries an executable gate.sh (guard-commit blocks red commits) and, where an LLM decides anything, an offline evals.sh; a 06:17 LaunchAgent runs both across the fleet and digests the result; the city board reads that digest, files its own morning report, and curates its own entries — deterministically gated." }
  ]}
  invariants={[
    "Raw research never enters the main window — subagents read; the loop receives conclusions.",
    "Disagreement is contractual: my framing is a hypothesis, and the critic is paid to attack it.",
    "Every phase boundary is a checkpoint a human verifies.",
    "Hooks fail open — they guard accidents, not deliberate obfuscation.",
    "Every token in every window is ledgered, with drill-down to what each subagent was fed.",
    "Verification is agent-runnable: gates and evals pass or fail without me, on every commit and every night.",
    "The board and I read the same digest — the site is the instrument panel, not a brochure."
  ]}
/>

---

The harness is why a one-person shop ships agents on payment rails with red-team suites. Same nervous system as the products, pointed at the producer — and increasingly, running without him.

<Ref to="projects/procurement-agent#decisions" note="the doctrine, applied to a product instead of the producer" />

<Ref to="writing/four-agents-one-nervous-system" note="the products' shared spine — this page is its fifth room" />

</Section>
