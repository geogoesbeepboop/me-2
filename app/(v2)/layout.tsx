/**
 * (v2) — the city. No site chrome: the landing and the ops room own
 * the viewport and bring their own bars, like the (x) explorations —
 * but this group is production, not a contact sheet, so no review rail.
 */
export default function V2Layout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
